# my-overlay/kustomization.yaml
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# resources:
#   - ./base/helm-chart

helmCharts:
- name: minecraft
  repo: base/helm-chart
  version: 1.1.25
  releaseName: cop
  namespace: helm-vs-kust
  valuesFile: base/helm-chart/my-values.yaml

patches:
  - path: overlay/patches.yaml
